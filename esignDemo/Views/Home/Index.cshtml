@{
    ViewData["Title"] = "Home Page";
    Layout = "_Layout";
}

<h1>Bienvenido a esignDemo</h1>
<p>Haga clic en el botón de abajo para generar un documento para firmar:</p>
<form asp-action="GenerateDocument" method="post">
    <button type="submit">Generar Documento</button>
</form>

@if (TempData["Message"] != null)
{
    <p>@TempData["Message"]</p>
}